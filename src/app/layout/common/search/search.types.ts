export interface productData {
  id: number;
  name: string;
  price?: number;
  discount: number | null;
  image?: string;
}

export interface newData {
  id: number;
  name: string;
  image?: string;
}
